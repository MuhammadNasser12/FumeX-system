# FumeX-system

FumeX-system is a local brand/inventory management system built as a web application. It runs on your local machine using Vite and React for the frontend, connected to a Supabase backend (PostgreSQL database with REST API).
